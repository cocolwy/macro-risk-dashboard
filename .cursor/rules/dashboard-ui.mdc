---
description: Dashboard UI conventions — animation, typography, and styling patterns for all pages
alwaysApply: true
---

# Dashboard UI Conventions

新增或修改看板页面时，遵循以下 UI 约定保持视觉一致性。

## 动画

### 1. 页面切换过渡（已内置于 Router.tsx）

`AnimatePresence` + `motion.div` 包裹路由内容，所有页面自动获得淡入 + 微上移过渡。**新页面无需额外处理。**

### 2. 卡片 / 内容区 Stagger 进场

新页面如果有多个卡片 / 图表，用 stagger 依次渐现：

```tsx
import { motion } from 'framer-motion';

const staggerContainer = {
  animate: { transition: { staggerChildren: 0.04 } },
};
const staggerItem = {
  initial: { opacity: 0, y: 10 },
  animate: { opacity: 1, y: 0, transition: { duration: 0.3, ease: [0.25, 0.1, 0.25, 1] } },
};

// 用法：
<motion.div className="charts-grid" variants={staggerContainer} initial="initial" animate="animate">
  <motion.div variants={staggerItem}><Card ... /></motion.div>
  <motion.div variants={staggerItem}><Card ... /></motion.div>
</motion.div>
```

**参数约定：**
- `staggerChildren`: 0.04s（40ms 间隔，不要超过 0.06）
- `duration`: 0.25–0.35s
- `ease`: `[0.25, 0.1, 0.25, 1]`（Apple ease-out）
- **禁止** 使用低阻尼 spring（`damping < 15`），会导致弹跳
- **禁止** 加 `scale` 动画，视觉上太吵

### 3. 数字计数动画

大数字（风险评分、价格等）使用 framer-motion `useSpring` 从 0 计数到目标值：

```tsx
import { useSpring } from 'framer-motion';

const spring = useSpring(0, { damping: 30, stiffness: 100 });
// spring.set(targetValue) 触发动画
// spring.on('change', v => setDisplay(Math.round(v)))
```

参考实现：`ScoreGauge.tsx` 中的 `AnimatedNumber` 组件。

### 4. Tooltip 跟随（已内置于 index.css）

`.recharts-tooltip-wrapper` 已全局设置 `transition: transform 0.18s cubic-bezier(0.34, 1.3, 0.64, 1)`，所有 Recharts 图表自动生效。**新图表无需额外处理。**

## 排版

- 字体栈：`-apple-system, BlinkMacSystemFont, 'SF Pro Display', 'SF Pro Text', 'PingFang SC', system-ui, sans-serif`
- 大标题 `letter-spacing: -0.02em`（紧凑感）
- 正文/说明 `letter-spacing: 0` 或 `0.005em`
- 数字/指标 `letter-spacing: -0.04em`（tabular 对齐感）
- `font-optical-sizing: auto`（已在 body 设置）

## 卡片样式

- 毛玻璃：`background: rgba(255,255,255,0.72); backdrop-filter: blur(16px) saturate(180%)`
- 圆角：`border-radius: 16px`（大卡片）/ `12px`（小元素）/ `8px`（badge/tag）
- 边框：`1px solid rgba(226,232,240,0.65)`
- 阴影：使用 CSS 变量 `var(--shadow-sm)` / `var(--shadow-md)` / `var(--shadow-lg)`
- Hover：`transform: translateY(-2px)` + shadow 升级，`transition: all 0.25s var(--spring-ease)`
- Active/Press：`transform: scale(0.985)`

## 颜色

使用 `index.css` 中定义的 CSS 变量，**不要硬编码新颜色**：
- 背景：`var(--bg-0)` / `var(--bg-1)` / `var(--bg-2)` / `var(--bg-3)`
- 文字：`var(--text-1)` / `var(--text-2)` / `var(--text-3)`
- 边框：`var(--border)` / `var(--border-blue)`
- 强调色：`var(--accent-green)` / `var(--accent-yellow)` / `var(--accent-red)` + 对应 `-bg` 变量
- 品牌粉/蓝：`var(--pink-*)` / `var(--blue-*)`

## 无障碍

- 所有动画必须尊重 `@media (prefers-reduced-motion: reduce)`（已在 index.css 全局处理）
- 不依赖纯颜色传达信息，配合文字/图标
